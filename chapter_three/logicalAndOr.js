var name = ''

// if left side true than exicute left side or if left side false than exicute right side
var fullname = name || 'Sajib Uzzaman'
console.log(fullname)

// if left side true than exicute right side
var isOk = true
isOk && console.log('Everything is ok')
